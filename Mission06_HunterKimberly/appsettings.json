{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "MovieConnection": "Data Source = JoelHiltonMovieCollection.sqlite" // give database a name or if it already exists, but database name
  }
}
